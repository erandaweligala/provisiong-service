server:
  port: 8089  # HTTPS port 8089 (changed from 8087)
  ssl:
    enabled: true
    key-store: classpath:aaacsg-server.p12
    key-store-password: Pass@456Bjd  # Replace with actual password
    key-store-type: PKCS12
    key-alias: aaacsg-server  # May need to verify this
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/plain
    min-response-size: 1024
  tomcat:
    max-threads: 200
    min-spare-threads: 20
    accept-count: 100
    connection-timeout: 5000

base-url:
  context: /axonect/aee/templates/v1.0/base-template
app:
  host: /MICROSERVICE_NAME/
  default:
    group-id: 1

  encryption:
    algorithm: AES
    secret-key: "Your16CharKey123"
  async:
    core-pool-size: 50
    max-pool-size: 200
    queue-capacity: 10000
    thread-name-prefix: "async-validation-"

  kafka:
    cluster:
      active: dc            # Primary cluster
    publish-to-both: false   # Only DC for now

    topic:
      user-events: dc-provisioning
      db-write: dc-provisioning
      service-events: dc-provisioning
      bucket-events: dc-provisioning
      super-template-events: dc-provisioning
      child-template-events: dc-provisioning

    publish:
      timeout-ms: 2000
      retry:
        enabled: true
        max-attempts: 2
      require-both-ack: false


log:
  identifierKey: UUID
  isDebug: true
info:
  app:
    name :  base-template
    msName : base-template
com:
  adl:
    dte:
      plugins:
        logging:
          enabled: false
          level: DEBUG
        rolling:
          policy: size
          size: 100000kb
        alarming:
          enabled: true
        msstat:
          enabled: true

springdoc:
  packagesToScan: com.axonect.aee.template.baseapp.application.controller
  pathsToMatch: /**
  swagger-ui:
    configUrl: /MICROSERVICE_NAME/v3/api-docs/swagger-config
    url: /MICROSERVICE_NAME/v3/api-docs
    disable-swagger-default-url: true
  api-docs:
    path: /v3/api-docs
spring:
  datasource:
    url: jdbc:oracle:thin:@aaa-dev.cn8eyeqma3tp.ap-southeast-2.rds.amazonaws.com:1521/ORCL
    username: aaa
    password: Aaa!89nky78D
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 50
      minimum-idle: 20
      connection-timeout: 5000
      idle-timeout: 30000
      max-lifetime: 180000
      pool-name: AAAHikariPool
      leak-detection-threshold: 60000
      data-source-properties:
        oracle.net.CONNECT_TIMEOUT: 5000
        oracle.jdbc.ReadTimeout: 10000

  kafka:
    bootstrap-servers:
      dc: kafka-headless.cluster-dc.svc.cluster.local:9092
      dr: kafka-headless.cluster-dc.svc.cluster.local:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        max.in.flight.requests.per.connection: 5
        compression.type: lz4
        batch.size: 32768
        linger.ms: 5
        buffer.memory: 67108864
        delivery.timeout.ms: 5000
        request.timeout.ms: 2000
        max.block.ms: 2000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.OracleDialect
        jdbc:
          batch_size: 20
          fetch_size: 100
        order_inserts: true
        order_updates: true
        generate_statistics: false

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

logging:
  level:
    root: INFO
    org.apache.kafka: INFO
    io.smallrye.reactive.messaging.kafka: INFO

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n"
    file: "%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n"

  file:
    name: /var/log/dte/${HOSTNAME:unknown}.log
    max-size: 100MB
    max-history: 10

  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 10
      clean-history-on-start: true

# Cache API Configuration
cache:
  api:
    base-url: http://airtel-aaa-accounting-management-service-service.airtel-aaa.svc.cluster.local:9905/api/v1/cache/addBucket/{bucketUsername}
    connect-timeout: 3000         # Connection timeout in milliseconds
    read-timeout: 5               # Read timeout in seconds
    write-timeout: 5              # Write timeout in seconds
    response-timeout: 5           # Response timeout in seconds
    max-connections: 100          # Maximum connections in pool
    pending-acquire-timeout: 10   # Timeout for acquiring connection from pool (seconds)

    # Retry Configuration
    retry:
      max-attempts: 2             # Maximum retry attempts
      initial-backoff: 1          # Initial backoff in seconds
      max-backoff: 3              # Maximum backoff in seconds

    # Request Timeout
    timeout:
      request: 5                  # Individual request timeout in seconds

thread:
  count:
    dbapiexecutor: 50

#management:
#  endpoints:
#    web:
#      base-path: /actuator
#      exposure:
#        include: "*"
#      path-mapping:
#        prometheus: prometheus
#  endpoint:
#    prometheus:
#      enabled: true
#  metrics:
#    export:
#      prometheus:
#        enabled: true


